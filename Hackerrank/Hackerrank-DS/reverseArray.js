function reverseArray(arr) {
  let reversedArray = [];
  
  for(let i = a.length - 1; i >= 0; i--) {
      reversedArray.push(a[i]);
  }
    
  return reversedArray;
} 

// Criar um variaval para armazenar o reverse array 
// Criar um loop para reverse array
  // i inicia com tamanho do array.length - 1 (inicia com o ultimo indice do array)
  // i tem que ser maior ou igual 0
  // i -- 
  // realiza o push do ultimo indice no novo array ate o loop ser finalizado

//Retorna o array reverse